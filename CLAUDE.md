# Git — Deploy

This repo deploys via **GitHub Pages** from the `main` branch.
Any push to `main` triggers an automatic deploy — no author restrictions.

Both Claude Desktop (danidevmash) and Claude Code (mashtakeec) can push freely.

## URL
- https://mashtakeec.github.io/gala-ads-dashboard/

## Notes
- `robots.txt` blocks all search engine indexing — the page is accessible only by direct link.
- The repo is public. Do NOT commit secrets, API keys, or credentials.
